# ToDoList

## 添加

- [x] 加入内置药品数据库

在本地添加`MedDB.json`，包含常见药品，程序内添加药品时支持快速搜索和按标签筛选

- [x] 药效时间数据（半衰期等）

内置药品数据库+添加时手动设置

- [x] 图标

SVG+PNG+ICO

- [x] 睡眠质量&时长&中断次数记录

这个对于双相来说挺重要的，睡眠质量使用0~5评分，时长按照记录的入睡/清醒时间自动计算，渲染为一个小长条，睡眠部分使用紫色，中断点是红色竖线，清醒部分使用灰色，长度固定，使用绝对时间比例，条的始点和末端就代表第一次入睡/最后一次醒来，中间就是中断部分（如有）

- [x] 个性化

情绪颜色（RGB选择）&数据点之间的连接方式（直线/曲线）&界面背景/强调色等自选/获取系统

- [x] Re1:个性化

自定义背景图片和渐变色

- [x] Re2:个性化

自定义药物标记颜色

- [x] 国际化多语言界面

使用 json 驱动

- [x] 数据库导入/导出（备份/还原用户数据）
- [x] 文档

包括面向用户和面向开发者的

- [x] 桌面端打包脚本

Microsoft® Windows™ 使用 Pyinstall 生成 .exe，GNU/Linux 使用 AppImage Builder 生成 .appimage

- [x] 边距

用于避免移动端全面屏导致的屏幕边缘控件难以点击

- [x] 界面缩放

允许在设置中调整软件界面整体的缩放级别（50%、75%、100%、125%、150%）来适配不同设备的屏幕大小

- [x] “设置”选项卡

数据库导入/导出和多语言就在这里选择了，还可以加上检查更新和关于信息

- [ ] 个人档案

基本上是双相类型（I型/II型/环性心境障碍/未特定），以后可以加上其它对就诊有用的信息

- [ ] 月/年度报告？

界面中可看，可导出为PDF/CSV

- [x] 手动修改药品变更日志
- [x] 重要事件

可以在记录中添加重要事件，该事件会在聚合视图上显示为一条始终显示的实心竖线，鼠标悬停以查看详细信息

- [x] 备忘录

一个简单的持久化文本输入框（在记录页）

- [x] 当前时间显示

在聚合视图上以灰色竖线显示当前时间

- [x] 前向药效计算

允许在药物库中设置每天固定的服药点，程序会假设在这些时间摄入药物，并计算药效，以为几天后的行程安排做准备，该功能也可以在聚合视图下方的复选框中随时开关

- [x] 悬停吸附

在足够靠近数据点的地方自动吸附

- [x] 抹掉数据

在设置中提供删除所有数据的按键（需二次确认）

- [x] 持久化视图当前查看位置

- [x] 事件距离现在的时间

在 记录→事件 （以及 记录→历史记录→编辑） 中添加一个复选框来选择是否为事件启用此功能，启用后在以下位置显示：

- 聚合视图鼠标指针悬停时的工具提示
- 记录→历史记录 中对应条目中的空白区域

如果是之前的事件可以当纪念日用（比如记录自己生病多少天），如果是未来的事件也可以当提醒用（比如规划复诊）

- [x] 药品耗尽预测

在 药品库→药品库存→剩余 中，除了百分比和总数，还可以显示该药品将在多少时间后耗尽（如果已设置固定服药时间）

- [x] Syncthing 模式

当内置数据存储有变动时，自动写出到一个特定文件，当检测到该文件有变更时，自动导入并覆盖内置数据存储，从而实现使用 Syncthing 的跨设备同步。

> "Everything is a File."（“万物皆文件。”）——Unix哲学语录

- [x] Syncthing 配置教程

编写图文指南，帮助用户配置 Syncthing 以实现自动跨设备同步，作为软件文档的一部分。

- [x] Google®Android™ 操作系统端桌面小部件

为 Google®Android™ 操作系统端（对于其它平台，直接标注不支持即可）加上桌面（启动器）小部件支持，因为打开软件再标记比较麻烦，在桌面放个小部件点一下一键记录，这样会方便很多，也能提升用户的使用意愿。（目前仅添加一键记录睡眠，点击后添加一条当前时间的睡眠/唤醒记录）

- [x] 桌面端桌面小部件

将一键睡眠记录支持扩展到桌面端，使用“--sleep-log-toggle”命令行参数+.lnk（Microsoft® Windows™ ）/.desktop（GNU/Linux）

- [x] 添加创建桌面小部件按钮

- [x] 上床与下床时间记录

在 记录→记录→睡眠 中，和入睡时间、清醒时间一样，可以填写上床时间与下床时间。在 概览→综合曲线 中在床上的时间用淡紫色显示，这个功能可能可以帮助那些虽然醒了但因为一些原因没法起床的人记录自己待在床上的时间

- [x] 滚动锁定

复选框，开启后不再能拖动视图，变为滑动选取数据，代替点选

- [x] 事件提醒功能

Google® Android™ 操作系统端独占，请求添加系统日程

- [x] 吃药提醒功能

Google® Android™ 操作系统端独占，请求添加系统闹钟

- [x] 自动记录服药

在设置里加开关，开启后全自动，到了设定的服药时间后自动生成记录

- [x] 更新文档并提供翻译版本
- [x] 补充的药效计算

程序目前会把第一次吃药记录之前的时间视为从没吃过药，然而，对于大部分中途才开始使用本软件的人来说，实际情况并非如此，因此，应允许在设置中添加一个输入框，选择开始吃药的日期，以及吃的什么药，应允许添加变更时间点，以应对中途调药。

- [x] 简化模式

在设置中添加一个简化模式允许以天或早中晚为单位记录，选中后替换记录页的情绪记录区域，对于天则删除时间选择器，早中晚则把时间选择器换成早中晚三个单选框，同时把吃药记录独立出来，与情绪睡眠和事件并列，以避开此修改带来的精度影响

- [x] 搜索和筛选功能

药品库和服药记录支持按标签过滤，历史记录和服药记录支持按时间段过滤，历史记录支持搜索

- [x] 睡眠记录的另一种显示形式

一个半透明的颜色区域，高度覆盖整个视图，宽度跟原逻辑一样，质量显示在最上方，在设置里加一个选项来设置是否使用这个新样式

- [x] 关闭动画

作为无障碍选项

- [x] 动态动画速度

如果记录比较多的话，在列表末尾等待渐入动画的时间会很长，应该添加一个记录越多动画就越快的设定

- [x] 性能：最大加载记录限制

添加一个只允许加载最近100条记录的限制，防止记录太多卡死或者OOM，如果超过则应该提示用户并建议通过自选时间范围来查看

- [x] 设置：搜索和折叠

允许设置中的各个分类手动折叠和展开，默认折叠，以及搜索设置项，因为当前设置界面太多的东西了，翻找起来比较麻烦

- [ ] 小事件

作为记录事件时的一个选项，让它没那么抢眼，渲染为短的一根灰色竖线

- [x] 服药记录点显示

之前有这个功能，但是自从服药记录跟情绪记录分离开之后就没有了，在视图里清晰的看到服药时间点还是挺有必要的

- [ ] 备忘录一键插入当前时间日期

添加一个按键，将当前的时间和日期一键插入到当前光标所在的位置

- [ ] 个性化：从背景图片自动取色

作为一个复选框，勾选后自动从图片中取平均颜色覆盖设定并且暂时禁用手动修改的控件，取消勾选后才恢复

- [x] 个性化：修改次要颜色

如“记录过多”提示的背景，药品余量条空白部分

- [x] 可读性：自动/手动切换黑白字体

当背景接近白色时，自动换成黑色字体，反之亦然，设置里放一个三态选择器，包含自动、黑色和白色三个选项，用于在某些自动检测不好的情况下手动调整

- [x] 简化模式增强

在简化模式下面加两个复选框，情绪和服药，勾选哪一个就简化对应的记录添加控件，未勾选的不简化。

- [x] 服药记录也允许添加备注

比如有时候忘记吃了补上就可以用这个标记一下

- [x] 个性化设置单独导出

这样子社区就可以方便快速地互相分享好看的主题而不担心泄露隐私了

- [x] 记录数据校验

防止不合理或重复的记录，比如先醒来再入睡的、入睡时间段长于在床上的时间段、在时间段上重复的睡眠记录、同一分钟内重复的情绪/服药记录等，应阻止添加这类数据

- [x] 自动备份

用于提升数据安全性，做成事件钩子形式（比如添加记录时执行），备份数据存储到软件外的文件夹，文件夹路径由用户选择，删除和恢复备份时需要有确认弹窗，还要有备份数量上限

~~之所以不做自动定时备份是因为需要后台保活，这个能不整尽量不整，因为考虑到用户设备上可能还有其它需要保活的软件，尽量减少这种软件的数量以避免资源争夺，同时我们一个小记录软件要保活也确实不太合适~~

- [x] 自动夜间构建（Nightly Build）

GitHub Action，当开发分支变动时自动编译并发布到 Release

- [x] 记录添加提示

添加记录成功时弹出提示，避免用户疑惑，在设置中可开关

- [x] 关于页

添加一个关于页面，显示软件图标、名称、描述和版本，一个检查更新按钮，还有一个按钮查看贡献者鸣谢单

## 修复：

- [x] 渲染（绘图）问题（颜色）

有时候，当情绪为+时，数据点之间的连接线仍然为蓝色

- [ ] Re1:渲染（绘图）问题（颜色）

如果数据里有混合期，聚合视图的连接线全都会变成蓝色，只有混合期那一小段是正常的

- [x] 移动端界面无法滑动问题
- [x] 移动端点击导出/导入崩溃
- [x] 翻译缺失（records.sleepForm.note）
- [x] 药物自定义颜色翻译更改后必须重启才生效
- [x] 缩放视图时查看区域会改变

缩放视图时当前查看区域会移动，应保持原地不动

- [x] 悬停位置检测与鼠标实际位置不一致

检测位置偏后且随视图滚动进度而加大

- [x] Microsoft® Windows™ 操作系统端无法使用

使用 PyInstaller 打包后的 Microsoft® Windows™ 操作系统端无法正常使用，报错 404 资源找不到

- [x] Re1:Microsoft® Windows™ 操作系统端无法使用

打包版卡死

- [x] 在“概览”选项卡的“聚合视图”（综合曲线）中，“在床上”时间段的渲染在某些情况下不完全、不正确或未渲染。

- [x] 获取系统主题

原本的需求是拉取操作系统强调色和背景色，如果没有再回退，理论上桌面端和移动端都有对应的接口，但是现在不起作用，点了只会变成默认颜色

- [x] CSS：药品库“添加耗尽提醒”按钮背景始终为白色，不受主题控制

- [x] 服药记录点显示和药效显示开关不同步

关闭药效显示时，服药记录点仍然会显示

- [x] 碳酸锂无法添加

问题的根本原因是表单验证被设计为只支持小数点后一位，但是内置药品数据库是从其它网站获取的，里面包含了精确到小数点后两位的数据，因此自动填入后表单验证无法通过，进而导致保存失败，可以通过将程序升级为支持更多小数点位数来解决

- 药物标记颜色恢复默认后选择器消失

在 设置→个性化→药物标记颜色 中点按 “恢复默认” 按钮后，上方的药品颜色选择器会消失不见

~~但是进行修复没有意义，因为内测的用户建议移除此设置项，改为在药品库新增或修改药品时选择~~

- [x] 不应同步自动备份路径

自动备份的路径不应该同步，不然通过 Syncthing 在不同操作系统间同步的时候可能会导致程序把自动备份文件写入到不该写入的目录中

## 修改：

- [x] 药效绘图

在概览的情绪波动图表下方创建新的图表，根据上面的药效时间数据自动计算，而不是手动输入

- [x] 对0值特殊处理为灰色而不是红色
- [x] 时间轴使用绝对比例（px/hr）

而不是数据点之间使用统一间隔

- [x] 药品库操作

把简单的“+1”和“-1”改成统一的“+/-”，点击后允许设置任意数量并注明理由

- [x] 情绪数据点详情显示逻辑

情绪数据点详情信息只有在悬停足够接近的一个范围内时才显示，而不是像现在这样始终显示离悬停位置最近的

- [x] 更简洁的药物添加逻辑

默认只显示预设，点击“手动添加/修改”按钮后才弹出原来的手动修改选项

- [x] 使用图标

在网页左上角和 Google® Android™ 操作系统端启动图标中使用`./assets/JimBDHubIcon256.png`

- [x] 在宽屏下也允许收起侧边栏
- [x] 更详细的时间轴

现在聚合视图下方的时间轴只显示每天八点，应该按照视图缩放自动调整为合适的细节等级

- [x] 纵轴标尺

纵轴标尺应该始终可见，而不是只在将视图滚动到最左/最右边时才可见

- [x] 使用内置对话框

制作并使用网页内置的对话框，而不是浏览器提供的

- [x] 视图缩放

1. 允许比默认更小的缩放
2. 添加最小/最大缩放限制和显示
3. 长按+/-按钮持续调整

- [x] 限制最大显示范围

限制视图的最大显示范围为一个月/星期内，超出则截断，这是为了防止在数据量大时导致卡死

- [x] 简化添加以前的药品变更记录的流程

现在想要添加之前的药品数量变更记录需要经过以下步骤：

1. 记住当前药品剩余总数
2. 通过 药品库→药品库存→操作→编辑→+/- 调整库存
3. 在 药品库→变更日志→编辑 中编辑时间
4. 在 药品库→药品库存→操作→编辑→当前剩余（总数） 中改回去

- [x] 用户友好的药品添加

现在通过内置药品数据库添加药品时，点选药品后列表直接变为“无匹配药品”，虽然此时数据已正确自动填入表单，但由于缺乏反馈导致用户疑惑，应添加明确的提示

- [x] 默认关闭情绪数据点连线

默认关闭情绪记录点之间的连线，仅显示离散数据点，并添加声明，以诚实反映数据未知区域的实际情况，避免视觉误导

在设置中添加开关，允许用户手动启用连线功能

添加声明提示，说明连线仅为视觉辅助，不代表实际未知数据

- [x] 药品数据和视图相关改动

1. 时间数据从单一确定值改成时间范围（x~y），避免现在这种只能取平均值的不严谨情况，同时在聚合视图中渲染两条药效曲线，分别是最高和最低
2. 新增字段：每片的剂量（mg等），这样就能在聚合视图中显示更具参考意义的浓度（mmol/L等），而不是现在的以片为单位（如1.23）

添加免责声明：这是经过简化后的预测，不能作为医疗建议

- [x] 事件距离当前的时间大于24小时时，应以天为单位
- [x] 聚合视图图例与下方控件优化

药多或者屏幕宽度小的话图例会全部挤在一起，下方的复选框们也是，可以添加自动换行

- [x] 聚合视图悬停tooltips优化

触控拖动视图时，工具提示的位置也要跟随视图实时更新，而不是留在触控点刚开始按下的地方

- [ ] 数据库升级

把当前代码中所有的旧数据格式兼容逻辑移动到一个统一的数据库升级管理器里，不再允许本地数据库里有旧数据格式，导入备份时，自动升级为最新的数据格式，升级失败时拒绝导入，假如当前数据库中有旧的数据格式，则在软件启动时告知用户需要进行的更改并要求备份，等用户手动备份后进行升级，如果升级失败则清空数据库，以上更改是为了更统一的管理旧数据格式，方便以后进行开发

目前，旧的数据格式包括：

- 捆绑在一起的情绪记录和服药记录（升级后应分离）
- 以片为单位计算的药物剂量和效果（升级后应将以前的数据改为当前药品库设置、允许做手动调整）
- 药物标记颜色设置（升级后按索引编号写入对应的药品中）
- 以及其它可能未列出的旧数据格式……

- [x] 药效显示

药物浓度低于0.01时不要继续渲染，直接截断对应曲线，如果跨页面的话只要以后没有再吃也不要再显示在图例里，也不要显示在工具提示里，不然有些之前吃过以后没再吃了的药也会一直显示，浪费屏幕空间

- [ ] 综合曲线比例尺优化

如果同时吃多种药，而药之间的剂量差的很多，剂量小的药在图表上会被压得看不见，首先改成每一种药都是动态范围，然后把右边的比例尺也改成独立的，每一种药的比例尺就用那种药的颜色，把手放上去或者鼠标悬停才显示文字（如“A药：100mg”），可以复用当前视图的tooltips代码

部分完成，仍需优化

- [x] 药品耗尽提示增强

- 药品库里新增一个叫做“添加耗尽提醒”的按钮，点击后请求添加到系统日程（Android Only，其它平台不显示）
- 聚合视图里添加对应药物颜色的虚线作为耗尽点显示，可以在几天前再加一个提示，这个提前的天数可以在设置里设置

- [x] 药品余量条优化

现在这种始终完整渐变的渲染方式其实没啥意义，删掉并改成截断渐变

- [x] 把背景色放回界面主题下

把背景颜色的选择器从“纯色”下改回和卡片、强调色并列，“纯色”下面不用放东西，因为哪怕背景选择了用图片或者渐变，其它控件的背景也要取此颜色

- [x] 移动“药物标记颜色”的设置位置

移除 设置→个性化→药物标记颜色，改为在药品库新增或修改药品时选择

- [x] 聚合视图加载限制

聚合视图（综合曲线图表）的加载限制应该每页独立，而不是所有页面加起来只有100条数据，不然这样的话基本没法看（动态载入，翻到那一页再加载那一页的数据，不要预加载所有页面）

- [x] 更人类可读的自动备份名

现在的自动备份在界面里有点“人类不可读”，这样吧，首先先把文件名格式改成“JimBDHub_AutoBackup_{操作}_{yyyyMMddhhmm毫秒}”（保留大小写，操作就是触发这个自动备份的原因），然后在软件里显示的时候自动去掉前面的“JimBDHub_AutoBackup_”前缀只保留后面的，并且给操作加上i18n（文件名的操作固定英文，只在 UI 翻译）

## 移除：

- [x] 示例数据

- [x] 单色药品余量条

应用户要求，该功能没有必要。。。

全软件最短命功能，刚做出来几分钟都没有，甚至还没推送就被内测用户否决了😅😅😅
