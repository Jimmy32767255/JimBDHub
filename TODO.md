# ToDoList

## 添加

- [ ] 个人档案

基本上是双相类型（I型/II型/环性心境障碍/未特定），以后可以加上其它对就诊有用的信息

- [ ] 月/年度报告？

界面中可看，可导出为PDF/CSV

- [ ] 小事件

作为记录事件时的一个选项，让它没那么抢眼，渲染为短的一根灰色竖线

- [ ] 备忘录一键插入当前时间日期

添加一个按键，将当前的时间和日期一键插入到当前光标所在的位置

- [ ] 个性化：从背景图片自动取色

作为一个复选框，勾选后自动从图片中取平均颜色覆盖设定并且暂时禁用手动修改的控件，取消勾选后才恢复

- [x] Export to .md

添加一个按钮来将所有记录导出成 Markdown 文件以供大语言模型分析，点击这个按钮的时候打开一个导出弹窗，里面可以调整要导出的记录数量，以避免超出上下文上限

- [x] 在软件中下载更新

在自动构建发布 Release 时，在产物中加上元数据`Metadata.json`，然后在软件里检查更新的时候，如果检测到新的版本就问用户是否下载，如果要下载，就先把这个元数据拉下来，里面会包含各个操作系统和架构的文件链接、校验码和该 Release 的频道，频道包括：

- 正式发行版（R,Release）
- 发布候选版（RC,Release_Candidate）
- 内部测试版（A,Alpha）
- 公共测试版（B,Beta）
- 开发版（D,Development）
- 金丝雀版（C,Canary）
- 夜间构建版（N,Nightly）
- 内部预览版（IP,Internal_Preview）

~~实际上用不到这么多频道，这是为后续预留，同时符合行业规范~~

软件拿到这些数据之后对比自己的运行环境找到正确的链接，下载下来之后通过校验码进行校验，如果校验通过就尝试启动安装，如果下载失败就提示用户“下载失败，是否前往浏览器手动下载？”。

总结改动点：

1. 自动构建发布 Release 时加上元数据
2. 软件中检查更新后增加一个更新按钮
3. 软件设置页加一个频道选择

## 修复：

- [ ] 渲染（绘图）问题（颜色）

如果数据里有混合期，聚合视图的连接线全都会变成蓝色，只有混合期那一小段是正常的

## 修改：

- [ ] 综合曲线比例尺优化

如果同时吃多种药，而药之间的剂量差的很多，剂量小的药在图表上会被压得看不见，首先改成每一种药都是动态范围，然后把右边的比例尺也改成独立的，每一种药的比例尺就用那种药的颜色，把手放上去或者鼠标悬停才显示文字（如“A药：100mg”），可以复用当前视图的tooltips代码

部分完成，仍需优化

- [x] 缩放相关调整

1. 将缩放上限由500%改为1000%
2. 移除最大和最小缩放的显示
3. 给“px/hr”加上翻译（如“像素/小时”）

## 移除：

暂无
