pywebview>=5.0
PyQt6
PyQt6-WebEngine
qtpy

# Windows Hello 生物认证（可选，仅 Windows）：
# 未安装时自动回退到 PowerShell 调用系统 WinRT API，无需额外依赖
# winsdk

# GNU/Linux 用户如果使用系统包管理器，Arch Linux 上可能还需要：
#   sudo pacman -S python-pyqt6 python-pyqt6-webengine
#
# GTK 备选方案（不需要 PyQt6）：
#   pip install PyGObject
#   同时需要系统安装 GTK3 / WebKit2GTK（Arch: webkit2gtk）
# Windows 与 macOS 通常无需额外安装 GUI 后端。
